{
  "name": "@scriptwriter/cli",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "tsc --watch",
    "prebuild": "rm -rf lib",
    "build": "tsc",
    "lint": "eslint --ext .ts --fix"
  },
  "dependencies": {
    "@scriptwriter/eslint-config": "*",
    "@scriptwriter/tsconfig": "*"
  }
}
