{
  "version": "0.0.0-development",
  "private": true,
  "scripts": {
    "build": "turbo run build --filter=./packages/*",
    "dev": "turbo run dev --filter=./packages/*",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,md,json}\"",
    "postinstall": "husky install",
    "release": "turbo run build --filter=./packages/* && changeset version && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.1",
    "@commitlint/cli": "^17.6.5",
    "@commitlint/config-conventional": "^17.6.5",
    "@scriptwriter/cli": "*",
    "@scriptwriter/eslint-config": "*",
    "@scriptwriter/quantumult": "*",
    "@scriptwriter/tsconfig": "*",
    "@turbo/gen": "^1.9.7",
    "@types/node": "^18.16.18",
    "eslint": "^7.32.0",
    "husky": "^8.0.3",
    "lint-staged": "^13.2.2",
    "prettier": "^2.5.1",
    "turbo": "latest",
    "typescript": "^5.1.3"
  },
  "name": "scriptwriters",
  "packageManager": "npm@9.7.1",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "lint-staged": {
    "*.ts": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{md,json}": [
      "prettier --write"
    ]
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/oustn/scriptwriters.git"
  },
  "engines": {
    "node": ">= 18"
  }
}
